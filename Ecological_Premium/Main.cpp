#include <iostream>

using namespace std;

int main()
{
    int n, f, fy, a, fef;

    do{
        cin >> n;
    }while(n >= 20 || n <= 0);

    for (int i = 0; i < n; i++)
    {
        do{
            cin >> f;
        }while(f >= 20 || f <= 0);

        int **fArray = new int*[4];

        for(int j = 0; j < 4; j++)
        {
            fArray
        }
    }
    
    for (int i = 0; i < 4; i++)
    {
        
    }
    

    return 0;
}