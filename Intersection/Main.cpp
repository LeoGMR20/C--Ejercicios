#include <iostream>
#include <stdio.h>

using namespace std;

int main()
{
    long x1, y1, r1, x2, y2, r2;

    while (true)
    {
        scanf("%ld %ld %ld %ld %ld %ld", &x1, &y1, &r1, &x2, &y2, &r2);
    }

    return 0;
}