#include <iostream>

using namespace std;

char vecinos[5000];

int main()
{
    int n, i;
    string reg;

    while (true)
    {
        cin >> n;

        if (n == 0)
            break;

        for (i = 0; i < n; i++)
        {
            cin >> reg;
            char res[reg.length()];
        }
    }
    return 0;
}